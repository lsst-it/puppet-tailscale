{
  "puppet_task_version": 1,
  "supports_noop": false,
  "description": "Installs tailscale",
  "parameters": {
    "authkey": {
        "required": true,
        "type": "String",
        "description": "The auth key use to configure the agent"
    },
    "ostype": {
        "required": false,
        "type": "String",
        "description": "The os type if tailscale fails to detect the os"
    }
  }
}
